import { styled } from 'styled-components'

export const HeaderContainer = styled.header`
  height: 18.5rem;
  img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    position: relative;
    z-index: -99;
  }
`
